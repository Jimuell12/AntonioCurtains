{
  "rules": {
    ".read": "true",
    ".write": "auth != null",
    
    "users": {
      ".indexOn": ["email"],

      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
